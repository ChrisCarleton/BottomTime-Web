describe('Viewing Other User\'s Log Books', () => {

	describe('As regular user', () => {
		// Can view public log books.
		// Can view friends-only log books if friended.
	});

	describe('As anonymous user', () => {
		// Can view public log books only.
	});

	describe('As admin', () => {
		// Can view all log books.
	});

});
